<h1><%= @post.title %></h1>

<%= link_to 'Add comment', new_post_comment_path(@post)  %>

<%= @post.comments.each do |comment| %>
  <p>comment was created <%= time_ago_in_words(comment.created_at) %> ago</p>
  <p><%= comment.remark %></p>

<% end %>